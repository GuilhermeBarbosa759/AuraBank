/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/MEU-BANCO-APP',
  assetPrefix: '/MEU-BANCO-APP', 
  images: {
    unoptimized: true,
  }
};

export default nextConfig;
