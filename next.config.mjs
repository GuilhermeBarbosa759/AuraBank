/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/AuraBank',
  assetPrefix: '/AuraBank', 
  images: {
    unoptimized: true,
  }
};

export default nextConfig;
